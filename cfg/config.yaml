defaults:
  - _self_
  - problem: bpp_online
  - override hydra/output: local

hydra:
  job:
    chdir: True

# The chosen algorithm
algorithm: hsevo
qd_type: embedding # embedding or bd
# LLM parameters
model: 'nvidia_nim/google/gemma-3-27b-it'
embedding_model: 'nvidia_nim/nvidia/llama-3.2-nemoretriever-1b-vlm-embed-v1'
temperature: 1 # temperature for chat completion

# Main GA loop parameters
max_token: 250000
max_fe: 100 # maximum number of function evaluations
max_gen: 10
pop_size: 10 # population size for GA
init_pop_size: 30 # initial population size for GA
mutation_rate: 0.5 # mutation rate for GA
timeout: 50 # timeout for evaluation of a single heuristic
warm_up: 0 
stop_condition: token # Supported conditions: 'token', 'fe', 'gen'
alpha: 0.99

# Harmony search
hm_size: 5
hmcr: 0.7
par: 0.5
bandwidth: 0.2
max_iter: 5

# behavior descriptor
bd_list: ["SLOC","cyclomatic_complexity", "halstead"]
bd_step: [3, 1, 50]